<%= turbo_stream_from "weather_measurements" %>

<div class="weather-container">
  <div class="weather-grid">
    <%= render "current_weather", current: @current %>
    <%= render "weekly_forecast", forecast: @forecast %>
  </div>
</div>
