// Injected by esbuild for nexrad-level-3-data (RandomAccessFile / seek-bzip).
import { Buffer } from "buffer"

if (typeof globalThis.Buffer === "undefined") {
  globalThis.Buffer = Buffer
}

export { Buffer }
